import { Button } from '@mui/material';
import { styled } from '@mui/material/styles';

export const Content = styled('div')`
  max-width: 1700px;
  margin: auto;
  padding: 2rem 0;
`

export const ButtonTableActions = styled(Button)`
  color: #000;

  .icon {
    cursor: pointer;
  }
`;